import { RegisterForm } from "@/components/auth/register-form"

export default function RegisterPage() {
  return <RegisterForm />
}

export const metadata = {
  title: "Register | Veritas",
  description: "Create a new Veritas account",
}